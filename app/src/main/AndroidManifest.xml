<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quranapp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
      <application
        android:name=".prefs.ContextApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo2"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuranApp">
        <activity android:name=".ui.sheikhHome.SheikhHomeActivity"/>
        <activity android:name=".ui.generateTest.GenerateTestActivity" />
        <activity android:name=".ui.showTest.ShowTestActivity" />
        <activity android:name=".ui.start.StartActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.studentHome.StudentHomeActivity" />
        <activity android:name=".ui.addPlan.AddStudentPlanActivity" />

    </application>

</manifest>